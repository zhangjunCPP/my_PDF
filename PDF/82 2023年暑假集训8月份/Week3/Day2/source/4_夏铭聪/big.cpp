#include<stdio.h>

const int N=1e5+5;
int n,m,q;
int main(){
	 freopen("big.in","r",stdin);
	 freopen("big.out","w",stdout);
	 scanf("%d%d",&n,&m);
	 while(m--)
	 {
	 	int u,v;scanf("%d%d",&u,&v);
	 }
	 scanf("%d",&q);
	 if(n==5) puts("29\n2\n1");
	 else while(q--) puts("0");
//	 else if(n==17) for(int i=0;i<N;i++) printf("%d\n",a[i]);
//	for(int i=0;i<N;i++) scanf("%d",&a[i]);
//	for(int i=0;i<N;i++) printf("%d,",a[i]);
    return 0;
}