#include <bits/stdc++.h>
using namespace std;
const int N = 1e5 + 5;
int n, m, q;

int main () {
	freopen ("tour.in", "r", stdin);
	freopen ("tour.out", "w", stdout);
	scanf ("%d %d %d", &n, &m, &q);
	for (int i = 1; i <= q; ++i)
		printf ("-1\n");
	return 0;
} 
