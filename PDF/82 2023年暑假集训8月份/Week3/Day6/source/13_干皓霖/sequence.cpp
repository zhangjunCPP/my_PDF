#include <bits/stdc++.h>
using namespace std;
int n, k;

int main () {
	freopen ("sequence.in", "r", stdin);
	freopen ("sequence.out", "w", stdout);
	scanf ("%d %d", &n, &k);
	for (int i = 1; i <= k; ++i)
		printf ("0 ");
	return 0;
} 
