#include <bits/stdc++.h>
using namespace std;
int n, m, q;

int main () {
	freopen ("a.in", "r", stdin);
	freopen ("a.out", "w", stdout);
	scanf ("%d %d %d", &n, &m, &q);
	for (int i = 1; i <= q; ++i)
		printf ("1\n");
	return 0;
} 
